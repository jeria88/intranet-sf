[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python seed_railway.py && python manage.py setup_utp_temuco && python manage.py setup_representante_temuco && gunicorn config.wsgi --timeout 120 --log-file -"
